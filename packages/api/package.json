{
  "name": "@rlbarn/api",
  "version": "1.0.0",
  "private": true,
  "author": "Jeff Miller",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "npx tsc -b",
    "build:new": "yarn run clean && npx tsc -b --force",
    "clean": "npx rimraf -rf ./dist"
  },
  "dependencies": {
    "@rlbarn/core": "1.0.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
